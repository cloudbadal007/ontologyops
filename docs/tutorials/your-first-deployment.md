# Your First Deployment

1. Install OntologyOps
2. Prepare your ontology
3. Run tests: `suite.run_all_tests()`
4. Create snapshot: `vc.create_snapshot()`
5. Deploy: `deployer.deploy()`
6. Monitor: `monitor.get_health_status()`
